using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PauseAndPlayMovement : MonoBehaviour
{
   /* public GameObject player;

    public void Stop()
    {
        player.GetComponent<PlayerMovement>().SetActive(false);
       // GameObject.FindWithTag("Player").GetComponent<Rigidbody2D>().isKinematic = true;
    }

    public void Restart()
    {
        player.GetComponent<PlayerMovement>().SetActive(true);
        // GameObject.FindWithTag("Player").GetComponent<Rigidbody2D>().isKinematic = false;
    }*/

}
